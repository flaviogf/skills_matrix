<main class="sessions-new">
  <%= form_with model: @session do |form| %>
    <h1><%= t '.title' %></h1>

    <div class="form-group">
      <%= form.label :email, t('.email.label'), class: 'form-label' %>
      <%= form.text_field :email, class: 'form-input', placeholder: t('.email.placeholder') %>
    </div>

    <div class="form-group">
      <%= form.label :password, t('.password.label'), class: 'form-label' %>
      <%= form.password_field :password, class: 'form-input', placeholder: t('.password.placeholder') %>
    </div>

    <%= form.submit t('.submit'), class: 'submit' %>
  <% end %>

  <% unless @session.errors.empty? %>
    <div class="errors">
      <ul>
        <% @session.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</main>
